# 🤖 Полностью автоматический деплой

## Что я сделаю:

1. ✅ Инициализирую Git репозиторий
2. ✅ Установлю Railway CLI
3. ✅ Подготовлю все файлы
4. ⚠️  Вам нужно будет:
   - Создать GitHub репозиторий
   - Авторизоваться в Railway (railway login)
   - Запустить финальный скрипт

## Пошаговая инструкция:

### Шаг 1: Создайте GitHub репозиторий

1. Зайдите на https://github.com
2. Нажмите "New repository"
3. Название: `hunslor` (или любое другое)
4. **НЕ** добавляйте README, .gitignore или лицензию
5. Нажмите "Create repository"

### Шаг 2: Запустите скрипт деплоя

```powershell
cd C:\hunslor
.\railway-deploy.ps1
```

Скрипт автоматически:
- Проверит Railway CLI
- Попросит авторизоваться (если нужно)
- Инициализирует Git (если нужно)
- Закоммитит все изменения
- Попросит добавить удаленный репозиторий
- Отправит код в GitHub
- Настроит Railway проект
- Добавит PostgreSQL
- Добавит переменные окружения
- Запустит деплой

### Шаг 3: Добавьте удаленный репозиторий

Когда скрипт попросит, выполните:

```powershell
git remote add origin https://github.com/YOUR_USERNAME/YOUR_REPO.git
git branch -M main
git push -u origin main
```

Замените `YOUR_USERNAME` и `YOUR_REPO` на ваши данные.

### Шаг 4: Авторизация в Railway

Если скрипт попросит авторизоваться:

```powershell
railway login
```

Откроется браузер для авторизации через GitHub.

### Шаг 5: Запустите скрипт снова

После добавления удаленного репозитория и авторизации:

```powershell
.\railway-deploy.ps1
```

Скрипт продолжит автоматическую настройку.

## Альтернатива: Ручной деплой через Railway Dashboard

Если предпочитаете использовать веб-интерфейс:

1. Закоммитьте и запушьте код в GitHub (скрипт сделает это)
2. Зайдите на https://railway.app
3. Войдите через GitHub
4. "New Project" → "Deploy from GitHub repo"
5. Выберите ваш репозиторий
6. Railway автоматически определит Next.js и начнет деплой
7. Добавьте PostgreSQL: "+ New" → "Database" → "Add PostgreSQL"
8. Добавьте переменные окружения в Settings → Variables

## После деплоя:

1. Получите URL: `railway domain` или в Railway Dashboard
2. Настройте Telegram Webhook (см. QUICK_DEPLOY.md)
3. Проверьте работу сайта

## Готово! 🎉
