# 🚨 КРИТИЧЕСКАЯ ОШИБКА: ОТСУТСТВУЕТ БАЗА ДАННЫХ

Логи Railway показывают ошибку:
```
Error: Environment variable not found: DATABASE_URL
```
Это означает, что **база данных НЕ подключена**. Приложение падает при запуске, потому что не может найти базу данных.

Я не могу исправить это через код, так как это настройка инфраструктуры Railway. **Вам нужно сделать это вручную (это займет 2 минуты).**

---

## ⚡ ЧТО НУЖНО СДЕЛАТЬ ПРЯМО СЕЙЧАС:

### 1. Добавьте базу данных (PostgreSQL)
1. Зайдите в ваш проект на [Railway Dashboard](https://railway.app/dashboard).
2. Нажмите кнопку **"+ New"** (или "Create").
3. Выберите **"Database"** -> **"Add PostgreSQL"**.
4. Подождите 30 секунд, пока база создастся.
   * *Railway автоматически добавит переменную `DATABASE_URL` в ваш проект.*

### 2. Добавьте остальные переменные
Чтобы сайт работал (и не упал снова после подключения базы), добавьте эти переменные в настройках вашего сервиса (вкладка **Variables**):

| Имя переменной (Name) | Значение (Value) |
| :--- | :--- |
| `JWT_SECRET` | `hunslor-railway-secret-key-production-2024-min-32-chars` |
| `TELEGRAM_BOT_TOKEN` | `8395474547:AAHM9sBQalUeNgvTyahr-6pdlaCaPW_0054` |
| `TELEGRAM_ADMIN_ID` | `8372817782` |
| `IMAGES_PATH` | `/app/public/images` |

*(Если вы не добавите эти переменные, сайт может открыться, но функции заказов и бота не будут работать).*

---

### 🚀 После этого:
Railway **автоматически** перезапустит сборку.
1. Сначала пройдет **Build** (сборка).
2. Потом начнется **Deploy** (запуск).
3. Благодаря моему обновлению кода, при запуске автоматически создадутся таблицы (`prisma db push`).

**Сайт заработает через 2-3 минуты после добавления базы данных.**
