[phases.setup]
nixPkgs = ["nodejs_20"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm run build"]

[phases.postbuild]
cmds = ["npx prisma db push --skip-generate || echo 'DB push completed or skipped'"]

[start]
cmd = "npm start"
