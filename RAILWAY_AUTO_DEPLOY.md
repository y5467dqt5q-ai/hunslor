# 🚀 АВТОМАТИЧЕСКИЙ ДЕПЛОЙ НА RAILWAY

## ✅ Все готово к автоматическому деплою!

### Что настроено:

1. **Конфигурация Railway:**
   - `nixpacks.toml` - правильная сборка с миграциями БД
   - `railway.json` - настройки деплоя
   - `Procfile` - команда запуска
   - `.railwayignore` - исключения для деплоя

2. **Исправления кода:**
   - ✅ Исправлена ошибка Prisma в `app/api/search/route.ts`
   - ✅ Все TypeScript ошибки исправлены
   - ✅ Конфигурация Next.js для продакшена

3. **Telegram бот:**
   - ✅ Работает через polling (не требует webhook)
   - ✅ API endpoints готовы

4. **База данных:**
   - ✅ Автоматические миграции через `prisma db push`
   - ✅ PostgreSQL будет подключен автоматически

## 📋 Настройка переменных окружения в Railway:

### 1. Откройте проект:
https://railway.app/project/a6111262-b4c7-468f-97e6-099305db819c

### 2. Добавьте переменные (Settings → Variables):

```
JWT_SECRET=hunslor-railway-secret-key-production-2024-min-32-chars
TELEGRAM_BOT_TOKEN=8395474547:AAHM9sBQalUeNgvTyahr-6pdlaCaPW_0054
TELEGRAM_ADMIN_ID=8372817782
OPENAI_API_KEY=YOUR_OPENAI_API_KEY_HERE
NODE_ENV=production
```

### 3. Добавьте PostgreSQL:
- "+ New" → "Database" → "Add PostgreSQL"
- `DATABASE_URL` добавится автоматически

### 4. Подключите GitHub:
- "+ New" → "GitHub Repo" → `y5467dqt5q-ai/hunslor`
- Выберите ветку: `main`

## ✅ После настройки:

Railway автоматически:
1. ✅ Соберет приложение (`npm run build`)
2. ✅ Применит миграции БД (`prisma db push`)
3. ✅ Запустит сервер (`npm start`)
4. ✅ Telegram бот заработает через polling

## 🔍 Проверка работы:

1. После деплоя Railway предоставит URL
2. Откройте сайт в браузере
3. Telegram бот будет работать автоматически через `/api/telegram/poll-updates`

## 🎉 Готово!

Все настроено для автоматического деплоя!
